import hallucinator as hl


#TODO 2d and 3d?
def plot_slope_field(points, x_range=x_range,
                     y_range=y_range,
                     foreground=foreground,
                     background=background,
                     resolution=resolution):
    """
    :param points: (x, y, f'(x, y))
    :return:
    """
